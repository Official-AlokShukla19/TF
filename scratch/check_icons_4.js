import * as icons from 'lucide-react';
console.log('Twitter:', Object.keys(icons).filter(name => name.toLowerCase().includes('twitter')));
console.log('Youtube:', Object.keys(icons).filter(name => name.toLowerCase().includes('youtube')));
console.log('Github:', Object.keys(icons).filter(name => name.toLowerCase().includes('github')));
