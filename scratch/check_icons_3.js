import * as icons from 'lucide-react';
console.log(Object.keys(icons).filter(name => name.toLowerCase().includes('social')));
console.log(Object.keys(icons).filter(name => name.toLowerCase().includes('brand')));
