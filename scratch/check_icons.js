import * as icons from 'lucide-react';
console.log(Object.keys(icons).filter(name => name.toLowerCase().includes('face')));
console.log(Object.keys(icons).filter(name => name.toLowerCase().includes('insta')));
console.log(Object.keys(icons).filter(name => name.toLowerCase().includes('linke')));
